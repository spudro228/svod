# Скопируй в inventory.ini и подставь свой сервер.
[svod]
svod.example.com ansible_user=root

[svod:vars]
# Домен, на который выпустится сертификат. Он должен уже указывать
# на этот сервер, иначе Let's Encrypt не сможет его проверить.
svod_domain=svod.example.com
svod_acme_email=ты@example.com
