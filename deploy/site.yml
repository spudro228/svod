---
# Полный проход: подготовить сервер и выложить свод.
# Идемпотентен — можно гонять сколько угодно раз.
- name: Свод целиком
  hosts: svod
  become: true
  roles:
    - base
    - docker
    - svod
